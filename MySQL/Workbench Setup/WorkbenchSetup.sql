 SELECT * FROM names
# SELECT name FROM names

# INSERT INTO names (name, created_at, updated_at)
# VALUES("Charan Karthik", NOW(), NOW())

# INSERT INTO names (name, created_at, updated_at)
# VALUES("George", NOW(), NOW()), ("Matt", NOW(), NOW()), ("Kevin", NOW(), NOW())

# UPDATE names SET name = "Ayssa", updated_at=NOW() WHERE id =3;
# DELETE FROM names WHERE id=2;
